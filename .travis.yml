sudo: required
services:
  - docker
before_install:
  - docker build -t shaddass/docker-react -f Dockerfile.dev .
script:
  - docker run -e CI=true shaddass/docker-react npm run test

language: generic

